export interface AddressItemProps {
    label: string;
    value: string;
    image: string;
    expanded: boolean;
    layoutId: string;
}

export interface PreviewImages {
    street: string;
    city: string;
    state: string;
    zip: string;
    country: string;
}